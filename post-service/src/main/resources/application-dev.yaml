bbs:
  db:
    host: mysql
    un: root
    pw: root
    database: post-service
    packet: com.example.post.domain.po

  es:
    host: elasticsearch

  mq:
    host: rabbitmq
    un: bbs
    pw: bbs-password

  redis:
    host: redis
    pw: admin123

  sentinel:
    host: sentinel-dashboard
    port: 8090
    tport: 7083

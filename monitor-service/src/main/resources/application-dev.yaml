bbs:
  db:
    host: mysql
    un: root
    pw: root
    database: sensitive-service
    packet: com.example.monitor.domain.po

  es:
    host: elasticsearch

  mq:
    host: rabbitmq
    un: bbs
    pw: bbs-password

  redis:
    host: redis
    pw: admin123
